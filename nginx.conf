location / {
    index index.php;
    try_files $uri /index.php?$query_string;
}

location = /assets {
    return 404;
}

location /assets/ {
    try_files $uri =404;
}

location /images/ {
    alias /app/storage/images/;
}
